import 'package:flutter/material.dart';
import '../../../features/news/domain/media_source.dart';

class MediaSources {
  MediaSources._();
  // Toutes les sources sont désormais chargées dynamiquement via dynamicMediaSourcesProvider
}
